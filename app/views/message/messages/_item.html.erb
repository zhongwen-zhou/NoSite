<% is_sender = message.sender == current_user %>
<div class="row">
	<div class="col-md-4 <%= 'col-md-offset-3' if is_sender %>">
		<%= is_sender ? '我' : message.sender.name %>说： <small><%= message.created_at.format_to_readable %></small>
		<p>
			<%= message.content %>
		</p>
	</div>
</div>

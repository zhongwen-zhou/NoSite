<%= simple_form_for([:admin, @sys_meta_hero]) do |f| %>
  <%= render 'shared/error_messages', :target => @sys_meta_hero %>
  <fieldset>
    <legend></legend>
    <%= f.input :name %>
    <%= f.input :talent %>
    <div class="form-actions">
      <%= f.submit %>
      <%= link_to 'Back', admin_sys_meta_heros_path %>
    </div>
  </fieldset>
<% end %>

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Modern Business - Start Bootstrap Template</title>
    <%#= stylesheet_link_tag "web", "data-turbolinks-track" => true %>
    <%= stylesheet_link_tag "web", "data-turbolinks-track" => true %>
    <%= csrf_meta_tag %>
  </head>

  <body>

  <%= render('share/web_top_nav') %>
  <%= render('share/web_spread') if @share_content[:web_spread] %>

  <%= yield %>
  <div class="container">
    <hr>
    <footer>
      <div class="row">
        <div class="col-lg-12">
          <p>Copyright &copy; Company 2013</p>
        </div>
      </div>
    </footer>
  </div><!-- /.container -->

  <%= javascript_include_tag "web", "data-turbolinks-track" => true %>

    <%= yield :scripts %>
    <script type="text/javascript">
      <% if current_user %>
        $(function(){
          $.get('<%= notice_path %>', {}, function(data){
            $('#rewards_count_span').text(data.rewards_count);
            $('#messages_count_span').text(data.messages_count);
          });
        });
      <% end %>
    </script>
  </body>
</html>

